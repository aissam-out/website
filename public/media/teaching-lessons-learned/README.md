# EMSI course media

Original PowerPoint decks were converted to WebP slide images and compressed PDFs under `public/slides/emsi/`.

Do not put large PPTX files back into `public/` for GitHub Pages deploys. Keep sources offline or outside the static export path.
