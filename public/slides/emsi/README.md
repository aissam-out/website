# EMSI ML/DL course slides

Published assets live in `public/slides/emsi/{deck-id}/`:

- `01.webp` … `NN.webp` — slide images used by the in-page viewer (lazy-loaded)
- `deck.pdf` — optional full-deck download

Heavy PowerPoint sources are not deployed. Re-generate from the original PPTX with LibreOffice + the conversion script used for this page if the decks need updating.
